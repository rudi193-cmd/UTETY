"""UTETY — the classroom-grade pedagogy + trust layer.

This package owns the *learner*: BKT mastery, STEM item progress, and the
local-first, on-device student-data core. Per the settled seam (build-plan
§5b.2): UTETY owns the learner and the content; the library (Jeles) owns the
sources. Student data never crosses off the device except as an optional,
consented sync built later as an explicit spine — never the default.
"""

__version__ = "0.0.1"
